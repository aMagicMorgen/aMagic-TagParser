/*CARD-BODY*/
{H3}
Синтаксис похож на PUG, добавлен атрибут name
{P}
Селектор записывается через / после #(id селектора). 
Пример: tag#id/name.class1.class2(attr) text. 
Если не указан тег — подразумевается div.

!!!!
h3.fw-semibold $H3
p $P